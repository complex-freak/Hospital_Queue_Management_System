{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["hammerjs", "node"],
    "baseUrl": ".",
    "paths": {
      "../../../shared": ["../shared"],
      "../../../shared/*": ["../shared/*"]
    }
  }
}
